<%- model_class = Album -%>
<div class="page-header">
  <h1><%=t '.title', :default => @album.name %></h1>
</div>

<div>
  <% for photo in @album.photos %>
    <div><%= image_tag photo.image_url(:thumb).to_s %></div>
    <div><%= photo.title %></div>
    <div>
    <%= link_to "edit" %>
    <%= link_to "remove", photo, method: :delete  %>
    </div>
  <% end %>
</div>

<div class="form-actions">
  <%= link_to "Add Photo", new_photo_path(:album_id => @album), :class => 'btn' %>
  <%= link_to t('.back', :default => t("helpers.links.back")),
              albums_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_album_path(@album), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              album_path(@album),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
